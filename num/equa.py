



def servey():
    #put after checking values
    day = input("day ")
    month = input("month ")
    year = input("year")

def broken_date(date):
    #broke int by one and return to list
    broken = int(date).split()

def find_birthday():
    #equlize broken nums from survey .return number
    # this first number

def find_numbers():
    #first number
    #find formula to equlize
    #and etc

def conv_two_to_one_numbers(number):
    #broke and plus
    n = str(number)
    return int(n[0]) + int(n[1])


def find_prof():
    #refactor after this shit code
    #equalize dual numbers
    first = day
    second = day + month
    third = broke_date(day) +broke(month)+broke(year)
    #convert to one
    l_first = conv_two_to_one_numbers(first)
    l_second = conv_two_to_one_numbers(second)
    l_third = conv_two_to_one_numbers(third)
    return l_first,l_second,l_third

